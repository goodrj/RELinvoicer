# Keep your real key in .env only. Do not commit .env to GitHub.
OPENAI_API_KEY=sk-your-key-here

# gpt-5.4 works well for reading small dimension text in drawing images.
OPENAI_MODEL=gpt-5.4

# Change this if another app is already using 3192.
PORT=3192
